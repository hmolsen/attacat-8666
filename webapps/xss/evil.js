setTimeout(function(){
	// DE: Cookies auslesen und in Variable speichern
	// EN: Read out cookies and save them in a variable
	
	// DE: URL erzeugen, die Cookie-Inhalt an den Logger schickt
	// EN: Concatenate a URL that is able to send the cookie content to the logger
	
	// DE: XMLHttpRequest erstellen
	// EN: create an XMLHttpRequest
	
	// DE: XMLHttpRequest als GET an die URL senden
	// EN: send the XMLHttpRequest as GET request to the URL from above
}, 500);
