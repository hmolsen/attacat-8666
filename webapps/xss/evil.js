setTimeout(function(){
	// DE: Cookies auslesen und in Variable speichern
	// EN: Read out cookies and save them in a variable
	
	// DE: URL erzeugen, die Cookie-Inhalt per Query-Parameter an den Logger schickt
	// EN: Concatenate a URL that is able to send the cookie content to the logger
	
	// DE: fetch-API verwenden, um GET-Request an logger zu schicken
	// EN: use the fetch-API to send a GET-Request to the logger
}, 500);
