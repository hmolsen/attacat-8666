setTimeout(function(){
	// Cookies auslesen und in Variable speichern
	// URL erzeugen, die Cookie-Inhalt an den Logger schickt
	// XMLHttpRequest erstellen
	// XMLHttpRequest als GET an die URL senden
}, 500);